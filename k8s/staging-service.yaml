apiVersion: v1
kind: Service
metadata:
  name: kubernetes-node-service
  namespace: staging
spec:
  type: NodePort
  selector:
    app: kubernetes-node-server
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 32052  # different NodePort!
